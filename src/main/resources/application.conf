server {
  host = "0.0.0.0"
  host = ${?SERVER_HOST}
  port = 8080
  port = ${?SERVER_PORT}
}

pokeapi {
  base-url = "https://pokeapi.co/api/v2"
  timeout-seconds = 10
  max-retries = 3
}

cache {
  max-size = 1000
  ttl-minutes = 60
}
